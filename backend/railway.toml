[build]
builder = "NIXPACKS"
buildCommand = "mkdir -p database storage/framework/{sessions,views,cache/data} storage/logs bootstrap/cache && cp .env.example .env && touch database/database.sqlite && php artisan key:generate --force 2>/dev/null || true && php artisan migrate:fresh --seed --force"
